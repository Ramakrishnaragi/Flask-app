apiVersion: apps/v1
kind: Deployment
metadata:
  name: flask-demo-app
  namespace: default
  labels:
    app: flask-demo-app
spec:
  replicas: 4
  selector:
    matchLabels:
      app: flask-demo-app
  template:
    metadata:
      labels:
        app: flask-demo-app
    spec:
      containers:
        - name: flask-demo-app
          image: ramakrishnaragi/flask-demo-app:b3ba532857e241be4c309f3ab084525cf2addf1f
          imagePullPolicy: "Always"
          ports:
            - containerPort: 5000
          env:
            - name: ENV
              value: production
          livenessProbe:
            httpGet:
              path: /
              port: 5000
            initialDelaySeconds: 15
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /
              port: 5000
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 256Mi
